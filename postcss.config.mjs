// Keep this monorepo isolated from any parent workspace PostCSS configuration.
// The public site uses authored CSS and does not require Tailwind processing.
const config = {
  plugins: {},
};

export default config;
